
When content exceeds the budget, ctx-handoff:
1. Always includes git state (branch, commits)
2. Includes diffs if they fit
3. Adds file contents in order of recent modification
4. Truncates when budget is reached
5. Shows a warning about truncation

## Output Format

Snapshots are saved in Markdown format for human readability and easy pasting into AI chats:

